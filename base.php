<?php
$PROMO_NAME = 'zhifubao';
require '/var/promo/base.php';
/*主文件内容
global $base_path;
$base_path = '/var/promo/' . $PROMO_NAME . '/';

function __autoload($class_name) {
    if(trim($class_name)) {
        $path = exec("find {$zhifubao}class/ -name {$class_name}.php");
        if($path && is_file($path)) {
            require $path;
        }
    } 
}
 */
